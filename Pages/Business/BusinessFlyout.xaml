<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GrassGol.Pages.Business.BusinessFlyout"
             BackgroundColor="#F4FBF2"
             HeightRequest="768"
             WidthRequest="250">
    
    <StackLayout Margin="24" HorizontalOptions="Center" Spacing="12">

        <Image Source="flyout_icon.png" WidthRequest="100" HeightRequest="74"/>

        <Button Style="{x:StaticResource FlyoutButton}" Text="Inicio"/>

        <Button Style="{x:StaticResource FlyoutButton}" Text="Reservas"/>

        <Button Style="{x:StaticResource FlyoutButton}" Text="Canchas"/>

        <Button Style="{x:StaticResource FlyoutButton}" Text="Historial"/>

        <Button Style="{x:StaticResource FlyoutButton}" Text="Facturación"/>

        <Button Style="{x:StaticResource FlyoutButton}" Text="Reportes"/>

        <Button Style="{x:StaticResource FlyoutButton}" Text="Empleados"/>


    </StackLayout>
</ContentView>